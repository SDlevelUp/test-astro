---
import BaseLayout from "../layouts/BaseLayout.astro";

const pageTitle = "A propos de moi";

const identity = {
    firstName: "Sarah",
    country: "France",
    occupation: "développeuse web",
    hobbies: [
        "le développement web",
        "la marche",
        "développer mes compétantces",
    ],
};

const skills = [
    "HTML",
    "CSS",
    "JavaScript",
    "React",
    "Astro",
    "Tailwind",
    "MongoDB",
];

/* Variable "happy" initialisée à la valeur booléenne "true", qui indique que quelque chose est heureux ou vrai. */
const happy = true;

/* Variable "finished" initialisée à la valeur booléenne "false", qui indique que quelque chose n'est pas fini ou n'est pas vrai. */
const finished = true;

/* Variable "goal" et est initialisée à la valeur numérique "3". 
Cette variable pourrait être utilisée pour représenter un objectif ou une cible à atteindre */
const goal = 3;

const skillColor = "black";
const fontWeight = "bold";
const textCase = "uppercase";
---

<style define:vars={{ skillColor, fontWeight, textCase }}>
    h1 {
        color: #c0c0c0;
        font-size: 64px;
    }
    .skill {
        color: var(--skillColor);
        font-weight: var(--fontWeight);
        text-transform: var(--textCase);
        list-style-image: url("../public/moon.png");
        transition: list-style-image 0.3s;
        line-height: 2;
    }
</style>

<BaseLayout pageTitle={pageTitle}>
    <h2>... et mon nouveau site Astro !</h2>
    <p>
        Je suis en train de travailler sur le tutoriel d'introduction d'Astro.
        C'est la deuxième page de mon site web, et c'est la première que j'ai
        construite moi-même !
    </p>
    <p>
        Ce site se mettra à jour au fur et à mesure que je compléterai le
        tutoriel, alors revenez régulièrement pour voir comment se déroule mon
        parcours !
    </p>

    {happy && <p>Je suis heureuse d'apprendre Astro</p>}

    {finished && <p>J'ai terminé ce tutoriel</p>}

    {
        goal === 3 ? (
            <p>Mon objectif est de finir en 3 jours.</p>
        ) : (
            <p>Mon objectif n'est pas 3 jours.</p>
        )
    }

    <p>Voici quelques faits à mon sujet</p>
    <ul>
        <li>
            Je m'appelle {identity.firstName}.
        </li>

        <li>
            Je vis en {identity.country} et je suis {identity.occupation}.
        </li>
        {
            /* vérifie si la personne a au moins deux passe-temps et affiche deux de ces passe-temps 
                 si c'est le cas. Si la condition est vraie, l'élément <li> est inclus dans la structure 
                HTML, sinon il est ignoré. */

            identity.hobbies.length >= 2 && (
                <li>
                    Deux de mes hobbies sont: {identity.hobbies[0]} et{" "}
                    {identity.hobbies[1]}
                </li>
            )
        }
    </ul>
    <p>Mes compétances sont:</p>
    <ul>
        {skills.map((skill) => <li class="skill">{skill}</li>)}
    </ul>
</BaseLayout>
